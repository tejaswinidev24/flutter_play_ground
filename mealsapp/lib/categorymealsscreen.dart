import 'package:flutter/material.dart';

class CategoryMeals extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reciepies'),
      ),
          body: Container(
        child: Center(
        child: Text('data'),
        )
        
      ),
    );
  }
}